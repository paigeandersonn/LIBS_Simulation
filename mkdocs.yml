site_name: libssim

theme:
  name: material
  palette:
    primary: indigo

plugins:
  - search
  - mkdocstrings:
      handlers:
        python:
          options:
            show_root_heading: true
            docstring_style: numpy

nav:
  - Home: index.md
  - API Reference: reference.md